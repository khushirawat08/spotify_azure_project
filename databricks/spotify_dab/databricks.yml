
bundle:
  name: spotify_dab
  uuid: af367f4c-842c-4568-a6cd-a4eb6318a236

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405618234150276.16.azuredatabricks.net
    variables:
      catalog: spotify_cata
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/harudhakar@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/harudhakar@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: spotify_cata
      schema: prod
    permissions:
      - user_name: harudhakar@gmail.com
        level: CAN_MANAGE
